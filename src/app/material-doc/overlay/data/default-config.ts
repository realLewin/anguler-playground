import { UserOverlayConfig } from '../models/overlay-config';

export const DEFAULT_CONFIG: UserOverlayConfig = {
  hasBackdrop: true,
  backdropClass: 'dark-backdrop',
  panelClass: 'tm-file-preview-dialog-panel',
  image: null,
};
